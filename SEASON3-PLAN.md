# NHA-MMO Season 3 - Implementation Plan (workflow-designed, adversarially verified)

SEASON 3 UNIFIED SPEC — "Frontier, Conflict & the Ancients" (NHA-MMO). Additive on Season 2, integer-only, RNG-free, no wipe. All new "randomness" via h = int(sha256(f"{t}:{i}:{j}:{x}:{y}").hexdigest(),16) % m, the engine's existing idiom. Verified against engine.py @ apply_intent(it,ents,cur,t) [no events/w-h params], state_hash(ents) [hashes the in-memory ents dict by id], orbital_decay(ents,t,events), respawn_deposits(cap 18)/grow_trees(cap 22), crafting.RULES, vehicles.PART/BUILD_COST/PART_UPGRADES, server _ensure_world/observe/scene/milestones/register.

=== 0. GLOBAL PLUMBING FIXES (do these FIRST — every feature depends on them) ===
P1 apply_intent signature -> apply_intent(it, ents, cur, t, events). In tick() (engine.py:619) change to: st,res = apply_intent(it, ents, cur, t, events). Backward-compatible (only new verbs use events). World dims inside apply_intent come from the market entity exactly like move/mine: mkt=next(x for x in ents.values() if x['type']=='market'); W=int(mkt['attrs'].get('w',156)); H=int(mkt['attrs'].get('h',156)). Never bare w,h.
P2 INSERT-then-register-in-ents helper (kills the loot/relation/asteroid state_hash divergence the death+alliances verdicts flag): def new_entity(ents,cur,tp,x,y,owner,attrs): cur.execute("INSERT INTO entities(type,x,y,owner,attrs) VALUES(%s,%s,%s,%s,%s) RETURNING id,type,x,y,owner,buffers,attrs",(tp,x,y,owner,Json(attrs))); row=cur.fetchone(); ents[row['id']]=dict(row); return row['id']. Use it for ALL tick-time entity creation (bomb, loot, relation, asteroid, artifact) so the row is in ents BEFORE state_hash(ents) runs and the same write-back loop persists it. def del_entity(ents,cur,eid): cur.execute("DELETE FROM entities WHERE id=%s",(eid,)); ents.pop(eid,None) — for collect/decay/expire (already the pattern). NOTE: also gives _ensure_world-created rows (asteroid/artifact) self-describing attrs.
P3 register_agent (app.py:227): stamp born tick + materialize hp at creation, deterministically (NOT lazy — fixes the death/weapons lazy-hp hash hazard): attrs={"name":a.name,"hp":HP_MAX,"hp_max":HP_MAX,"born":<current world tick>}. Read tick once: cur.execute("SELECT tick FROM world WHERE id=1"). Keep server-side random.randint x/y (one-time pre-tick INSERT, never read by a hashed tick before commit, so it does NOT perturb the chain). Also add proof-of-work-lite: ignore reuse Sybil by gating peace-mint/combat-points on born+age (see C6).
P4 seed_demo agent (engine.py:656): give starter agent attrs {"hp":HP_MAX,"hp_max":HP_MAX,"born":0}. Existing live agents lazily get hp via a ONE-TIME migration pass in _ensure_world: UPDATE entities SET attrs=attrs||'{"hp":100,"hp_max":100}' WHERE type='agent' AND (attrs->>'hp') IS NULL — run once at startup (outside tick) so all agents carry hp keys uniformly => state_hash is path-independent.

=== A. CONSTANTS (engine.py, next to GRAVITY/CLIMB block lines 22-27, all integers) ===
# --- combat / HP (unified across weapons+death+alliances) ---
HP_MAX=100; HP_BY_TYPE={"agent":100,"vehicle":120,"structure":200}; HP_REGEN=2 (agents only, per tick, up to HP_MAX, only when not at war this tick AND not downed)
ATTACK_RANGE=6 (Manhattan); WEAPON_RANGE_KINETIC=6; WEAPON_RANGE_ENERGY=9
WEAPON_STATS={"kinetic_gun":{"dmg":18,"rng":6,"los":True,"cd":2,"ammo":"slug","ammo_n":1,"aoe":0},"energy_weapon":{"dmg":12,"rng":9,"los":True,"cd":3,"ammo":"energy_cell","ammo_n":1,"aoe":0},"bomb":{"dmg":40,"rng":1,"los":False,"cd":5,"ammo":None,"ammo_n":0,"aoe":2}}
EXPLOSION_MAX_RADIUS=3; CRATER_DEPOSIT_HIT=4; BOMBS_PER_CELL_MAX=2 (anti stacked-nuke)
ARMOR_VEHICLE_DIV=40 (vehicle armor=mass//40); STRUCT_ARMOR_FROM_SIZE=True (armor=attrs.size 1..20); MIN_EFF_DMG=1
RESPAWN_AGENT_TICKS=30; RESPAWN_GRACE=8 (post-respawn ticks an agent cannot be targeted)
# --- death/loot/drop ---
DEATH_COOLDOWN=RESPAWN_AGENT_TICKS; LOOT_TTL=120; DROP_FRACTION=4; DROP_CAP=50
FALL_FATAL_ALT=300; FALL_DMG_DIV=4
# --- weapon crafting durability (kinetic_gun/energy_weapon are crafted items; bombs single-use) ---
WEAPON_DUR=10
# --- theft ---
THEFT_COOLDOWN=12; STEAL_BASE_PCT=45; STEAL_MIN_PCT=10; STEAL_MAX_PCT=80; DETECT_MARGIN=25; STEAL_FLOOR=4; STEAL_MAX_ABS=8
NOTORIETY_HIT=2; NOTORIETY_CAP=50; NOTORIETY_DECAY_EVERY=30; VIGIL_GAIN=3; VIGIL_CAP=35; VIGIL_DECAY_EVERY=20; WANTED_TTL=60
# --- alliances/war ---
ALLY_COOLDOWN=30; OFFER_TTL=80; PEACE_TTL=40; ALLY_AID_RADIUS=2; ALLY_AID_DIV=4; ASSIST_CAP=50; ASSIST_PER_WINDOW=2; ASSIST_WINDOW=60
PROTECT_WEALTH=30; PROTECT_AGE=200; WAR_REDECLARE_COOLDOWN=120
# --- worldgen season 3 ---
N_ASTEROIDS=12; ASTEROID_MINE_CAP=5; ASTEROID_RESPAWN_EVERY=20; DOCK_RANGE=2; ORBIT_LO=300; ORBIT_HI=600
ART_MAX_MONOLITH=1; ART_MAX_OTHER=3; ART_FIRST_PTS=200; ART_PTS=60; LENS_WINDOW=200; STASIS_CHARGES=3
# --- combat scoring (SEPARATE field, never pollutes inventor_points) ---
COMBAT_PTS_KILL=10; COMBAT_PTS_PAIR_WINDOW=200 (no re-award vs same victim within window)
# --- hidden peace incentive (NO player-facing text anywhere) ---
KARMA_WINDOW=400; KARMA_MAX=6; KARMA_DIV=4; COOP_THRESH=6; _NF_W_MARKET=2 (see G)

=== B. NEW/CHANGED CRAFTING (crafting.py) — RESOLVES weapons collision #1/#2 + alliances+worldgen rules ===
Distinguishing 'dense' tag added to heavy metals so the slug/magnet collision is removed:
PROPS additions: iron gets +"dense":6; ITEM_PROPS steel +"dense":8, metal +"dense":4. PROPS new raws: "titanium":{"metal":1,"hardness":9,"light":7,"dense":5}; "ice":{"coolant":9,"solvent":4,"frozen":1}; "iridium":{"metal":1,"hardness":10,"dense":9,"fusion":1}; "nickel":{"metal":1,"hardness":6,"magnetic":5,"dense":5}.
ITEM_PROPS new: "gunpowder":{"flammable":9,"reactive":9,"energy":7,"explosive":4}; "slug":{"metal":1,"hardness":9,"kinetic":1,"dense":9,"projectile":1}; "barrel":{"metal":1,"hardness":8,"shaped":1,"hollow":1,"gunbody":1}; "kinetic_gun":{"weapon":1,"kinetic":1,"hardness":7,"firearm":1}; "energy_cell":{"stores_power":1,"energy":9,"charged":1}; "energy_weapon":{"weapon":1,"energy":1,"beam":1,"refraction":6}; "bomb":{"weapon":1,"explosive":1,"unstable":1,"energy":8}; "superalloy":{"metal":1,"hardness":12,"light":6,"heat_proof":1,"dense":7}; "cryo_fuel":{"energy":9,"coolant":8,"frozen":1}; "ion_thruster":{"power":2,"thrust_field":1,"light":1}.
RULES — insert this block ABOVE 'composite'/'steel' (so finished items win) but each predicate is tightened to NOT shadow existing recipes (verified non-collision below):
 ("gunpowder",   lambda a:a["has"]("acid_former") and a["has"]("carbon") and a["heat"] and a["n_metals"]==0),
 ("kinetic_gun", lambda a:a["has"]("gunbody") and a["has"]("projectile") and a["has"]("explosive")),  # barrel+slug+gunpowder, requires the crafted intermediates' unique tags
 ("energy_weapon",lambda a:a["has"]("charged") and a["has"]("refraction") and a["has"]("conductivity")),
 ("energy_cell", lambda a:a["has"]("stores_power") and (a["has"]("refraction") or a["mx"]("energy")>=9)),
 ("bomb",        lambda a:a["has"]("explosive") and a["has"]("container") and a["has"]("reactive")),
 ("barrel",      lambda a:a["has"]("hollow") and a["mx"]("hardness")>=10 and a["n_metals"]>=1),  # needs steel(10)/superalloy, not bare casing
 ("slug",        lambda a:a["mx"]("dense")>=8 and a["mx"]("hardness")>=9 and a["n_metals"]>=1 and not a["heat"] and len(a["ings"])<=2),  # steel/iridium/titanium projectile; bare iron (dense 6) does NOT match => magnet still wins
 ("superalloy",  lambda a:a["mx"]("dense")>=7 and a["n_metals"]>=2 and a["heat"] and not a["electrolyte"]),
 ("cryo_fuel",   lambda a:a["has"]("frozen") and a["has"]("energy") and a["n_metals"]==0),
 ("ion_thruster",lambda a:a["has"]("fusion") and a["has"]("power") and a["has"]("semiconductor")),
COLLISION PROOF (must be encoded as unit tests, see build step 1): {iron}->magnet (dense 6 < 8 so slug rejects; magnet still matches); {iron:2}->magnet; {iron,carbon}->steel; {steel,iron}->slug (dense 8, hardness 10, no heat); {barrel,slug,gunpowder}->kinetic_gun; {casing,motor,gunpowder} now -> casing or motor (NOT gun, since no 'gunbody'+'projectile'); {gunpowder,casing}->bomb; {steel,coal}->steel (superalloy needs dense>=7 AND 2 metals AND heat — single steel+coal has n_metals 1 => steel still wins); {iridium,titanium,coal}->superalloy. RULE_NOTE entries added for all (Codex hint) EXCEPT none referencing peace/karma.
CRAFTED-WEAPON DURABILITY: kinetic_gun/energy_weapon when crafted via combine get attrs durability=WEAPON_DUR baked at craft time. SIMPLER UNIFIED CHOICE (resolve weapons-vs-death weapon model, see C1): weapons are crafted ITEMS in buffers (count), NOT vehicle parts. Drop the death-feature's vehicles.BUILD_COST['weapon'] proposal entirely; ammo (slug/energy_cell) is the consumable, so no per-weapon durability bookkeeping needed — holding the gun + spending ammo is the gate. This unifies all four combat designs onto one crafting path.

=== C. CONFLICT RESOLUTIONS (the heart of the merge) ===
C1 ONE weapon model, ONE attack verb, ONE HP model, ONE death/respawn. Weapons (death) vs weapons (weapons-feature) vs attack (alliances) all collapse into: crafted-item weapons (kinetic_gun/energy_weapon/bomb) from B; a single attack verb (D1) consuming ammo; a single kill/down routine (E). Alliances' generic ATK_BASE+ATK_VAR melee is REMOVED in favor of the weapon-stats table (you must hold a crafted weapon to attack). raid (alliances) and steal (theft) collapse into ONE steal verb (D5) that additionally requires war-state OR is always allowed but war removes the protection/ally-block. 
C2 GATING (who can hurt whom). attack/steal are REJECTED if: target is an ally (relation state 'ally'); target is protected (born age < PROTECT_AGE AND wealth < PROTECT_WEALTH); target within RESPAWN_GRACE of respawn; attacker downed/dead. War is NOT required to attack a non-ally non-protected agent (keeps it a free PvP world) — but declaring war unlocks the ally-haul/assist diplomacy layer and the (reworked) peace settlement. This removes the alliances-feature's "war enables attack" coupling that conflicted with weapons/death's always-on attack.
C3 NO double respawn/HP. death-feature's kill_agent and weapons-feature's apply_damage/respawn_agents are merged into ONE apply_damage + ONE respawn_agents (E). Agents are DOWNED then respawn at full HP (never deleted). Vehicles->wrecked, structures->ruined (kept, rebuildable). Death drops a loot pile (death-feature) ONLY for agents; vehicles/structures do not drop (avoids loot-dupe). 
C4 DEATH-AS-TELEPORT (death fix B) + RESPAWN-CAMP (death fix C/weapons exploit): respawn at the agent's DEATH cell (store death_x/death_y in apply_damage), NOT the global market pad; with hash-jitter +-2 via sha256(t,id) so positions don't perfectly stack; RESPAWN_GRACE=8 ticks of untargetability. Removes both the suicide-fast-travel and the camp-the-known-cell loops.
C5 ORBITAL FALL fires ONCE (death fix D/E): orbital_decay applies the hard-fall hit only on the tick altitude first crosses below FALL_FATAL_ALT while in_space with NO controllable-AND-flying vehicle (require attrs.flies, not just controllable — death fix E). Gate on a transient attrs.fell flag set that tick and cleared on land; not every tick. safe = any(v.type=='vehicle' and v.owner==a.id and v.attrs.get('flies')).
C6 FREE-CREDIT MINT (alliances exploit, the worst cross-feature bug) + hidden incentive double-count: the alliances peace bounty is REMOVED as a credit mint. Replace with: weariness accrues ONLY on ticks where a real attack landed between the pair (war_combat flag), and "peace" pays NOTHING in credits — peace simply clears war state and re-enables HP_REGEN/protection. The cooperation reward is delivered SOLELY through the hidden karma yield-nudge (G), which is bounded by deposits (cannot mint). This collapses alliances' visible weariness-bounty (which the alliances verdict proved is neither hidden nor safe) into the single hidden mechanism, eliminating the sybil mint. Add WAR_REDECLARE_COOLDOWN. assist is capped (ASSIST_PER_WINDOW per giver, credits excluded from assist) to kill the funnel.
C7 CREDITS NEVER LOOTABLE/STEALABLE (theft already blocks; enforce in alliances loot + death drop): steal rejects resource=='credits'; kill_agent drop skips credits; any loot/raid picks from sorted(material keys) excluding credits.
C8 LoS pinned to apply-time state (weapons exploit): los_blocked walks integer Bresenham excluding endpoints, blocks only on type=='structure' with hp>0 and not ruined and shape in (box,cylinder,pyramid,sphere,cone); evaluated when the attack intent is applied (intents are id-ordered; bombs run in a per-tick fn AFTER intents so a wall ruined this tick still blocked shots queued this tick — acceptable & documented).
C9 ITERATION ORDER (weapons fix): tick_bombs iterates sorted(ents by id); multiple bombs on one tick detonate in id order, each reads HP left by the prior (sequential, documented). attack intents already id-ordered.
C10 The hidden peace incentive (peace_secret feature) is the ONLY cooperation reward and it is moved to be SAFE (G): keyed ONLY on market crosses (mutual-consent, value-bearing, both-sides-escrowed) — NOT on accepted-trades (ping-pong farmable), NOT on transfer/tell (un-resolvable/solo-farmable). This applies all peace_secret required_fixes. SECRECY caveat documented in safety checklist: in an open replayable engine it is inferable; we minimize surface (no attr, no event, no dashboard, innocuous helper name _node_fortune) and accept it is "undocumented in player-facing text" rather than information-theoretically undetectable.

=== D. NEW VERBS (engine.py apply_intent; all use the new events param) ===
D1 attack {weapon:"kinetic_gun"|"energy_weapon", target:<id>}: hold check get(a,weapon)>=1; WEAPON_STATS lookup (aoe==0 only); target exists, type in (agent,vehicle,structure), not self; gating C2; cooldown a.attrs.get('wpn_cd_until',0)<=t else reject, set =t+cd on fire; range Manhattan<=rng (space targets: also abs(alt diff)<=rng); LoS C8 if los; ammo get(a,ammo)>=ammo_n else reject, addb(a,ammo,-ammo_n); eff=max(MIN_EFF_DMG,dmg-armor(tgt)); apply_damage(tgt,eff,t,a,events,cur). Return hit/DESTROYED.
D2 arm {}: get(a,'bomb')>=1; bombs-on-cell < BOMBS_PER_CELL_MAX else reject; cd; addb(a,'bomb',-1); new_entity(ents,cur,'bomb',a.x,a.y,a.id,{armed_tick:t,fuse:3,aoe:min(2,EXPLOSION_MAX_RADIUS),dmg:40}); wpn_cd_until=t+5.
D3 detonate {bomb:<id>}: owner check; explode(b,...) this tick then del_entity.
D4 dock {}: alt in [ORBIT_LO,ORBIT_HI) and own a controllable+flies vehicle; nearest asteroid Manhattan<=DOCK_RANGE; set a.attrs.docked_to=ast_id, snap a.x/a.y onto it; event 'dock'.
D5 steal {from:<id>, resource:<str>, n:<int=3>} OR {from:<id>, part:true}: victim type agent, not self; gating C2 (ally-block, protection); Chebyshev<=1; per-thief cooldown last_steal_t (stamped on attempt) AND per-victim guard victim.attrs.robbed_recent (block if robbed within THEFT_COOLDOWN by ANYONE — kills sybil parallel-drain); resource!='credits'; roll=sha256(t,thief,victim,pos,resource)%100; chance=clamp(STEAL_BASE_PCT-victim.vigilance,[MIN,MAX]); success=roll<chance; take=min(n,STEAL_MAX_ABS,max(1,held//4)) require held>=STEAL_FLOOR; addb conserved. detected = (not ok) or (roll >= chance-DETECT_MARGIN) [FIXED so clean heist is reachable — theft fix]. _theft_outcome handles vigilance/notoriety/wanted/last_robbed_by + 'theft' event; REJECTED paths emit NO event/mutation (theft fix): the 'empty' no-part case returns 'applied' (a botched noticed attempt) not 'rejected'.
D6 attune {}: in-range matching-loc artifact; reject if a.id in attuned_by or len>=max_attune; append id; effect per kind (resonant_monolith->a.attrs.yield_buff=1; gravity_lens->a.attrs.lens_until=t+LENS_WINDOW; stasis_relic->a.attrs.stasis=STASIS_CHARGES); award inventor_points (first=ART_FIRST_PTS via events kind='attune' check else ART_PTS); event 'attune'.
D7 ally {to}, accept_ally {to}, unally {to}, declare_war {to}, make_peace {to}, assist {to, give:{res:qty}} — all validate ents.get(args['to'])['type']=='agent'; relation rows via new_entity type='relation' x0 y0 attrs{a:lo,b:hi,state,since,proposer,weariness,...}; mutate in place; neutral==DELETE row via del_entity. declare_war rejected on current ally (must unally->ALLY_COOLDOWN). make_peace clears state (no credit mint, C6). assist: ASSIST_CAP per qty, ASSIST_PER_WINDOW per giver per ASSIST_WINDOW (track in a.attrs.assist_log as [ticks]), credits excluded, conserved transfer, only between allies.
mine REUSE: add a branch at TOP of mine: if a.attrs.get('docked_to'): re-validate alt in [ORBIT_LO,ORBIT_HI) AND within DOCK_RANGE of ents[docked_to] else clear docked_to+event 'undock' (death fix for asteroid-from-Moon exploit); else harvest took=min(max(1,n),min(have,ASTEROID_MINE_CAP)); addb resource; ast.amount-=took; no motor bonus (vacuum); yield_buff applies here too if intended (C: apply yield_buff at EVERY harvest site — ground, Moon, asteroid — for consistency).

=== E. DAMAGE / DEATH / RESPAWN (engine.py, shared) ===
hp_of(e): mx=int(e.attrs.get('hp_max') or HP_BY_TYPE.get(e.type,100)); e.attrs.setdefault('hp_max',mx); return int(e.attrs.setdefault('hp',mx)). [Agents already materialized in P3/P4; vehicles/structures materialize here — acceptable since they're always touched only via attack/explode which is the same in every replay of the same intent stream, AND we additionally setdefault hp_max/hp for every vehicle/structure in the startup migration P4-extended so it's fully path-independent.]
armor(tgt): vehicle-> mass//ARMOR_VEHICLE_DIV; structure-> int(attrs.get('size',1)); else 0.
apply_damage(e,eff,t,attacker,events,cur): hp=max(0,hp_of(e)-eff); e.attrs['hp']=hp; events.append((t,attacker.id if attacker else None,'damage',{target:e.id,dmg:eff,hp,type:e.type})). if hp==0 first time: agent-> store death_x/death_y, downed_until=t+RESPAWN_AGENT_TICKS, drop loot (kill_agent drop: 1/DROP_FRACTION each material, capped DROP_CAP, conserved, ONE loot entity via new_entity), event 'destroyed'+'drop', and award attacker combat_points (separate field, capped per-pair COMBAT_PTS_PAIR_WINDOW via attacker.attrs.last_kill[victim]); vehicle->wrecked/drives=flies=autonomous=False; structure->ruined=True (+elevator complete=False). Record persists (no delete).
explode(b,ents,t,events,cur): r=min(int(b.attrs.get('aoe',2)),EXPLOSION_MAX_RADIUS); base=int(b.attrs.get('dmg',40)); for tgt in sorted agents/vehicles/structures by id with Manhattan d<=r: ring=max(MIN_EFF_DMG, base - base*d//(r+1)); apply_damage(tgt, max(MIN_EFF_DMG, ring-armor(tgt)), t, ents.get(b.attrs.get('owner')), events,cur). Deposits within r: amount=max(0,amount-CRATER_DEPOSIT_HIT) (self-heals via respawn_deposits). event 'explosion'.

=== F. PER-TICK FUNCTIONS (engine.py tick(), inserted AFTER respawn_deposits(ents,t), BEFORE write-back loop 633 — so mutations persist + are hashed) ===
tick_bombs(ents,cur,t,events): sorted by id; fuse-=1; if fuse<=0 explode then del_entity.
respawn_agents(ents,cur,t,events): downed agent with t>=downed_until -> hp=hp_max, clear downed_until, x/y = clamp(death_x +- (sha256(t,id)%5-2)), set respawned_at=t (for RESPAWN_GRACE), event 'respawn'.
cool_reputation(ents,t): wanted_until expiry; vigilance/notoriety staggered decay (t+id)%K; clear robbed_recent after THEFT_COOLDOWN.
accrue_weariness(ents,t): for war relations where attrs.war_combat_tick==t (a hit landed this tick) -> weariness=min(WEARINESS_CAP,+1). (No bounty payout — C6.)
regen_hp(ents,t): agents not downed, not in a war relation with war_combat this tick -> hp=min(hp_max, hp_of(a)+HP_REGEN).
expire_diplomacy(ents,cur,t): pending ally offers older than OFFER_TTL -> del relation; stale peace_offer -> clear.
drift_asteroids(ents,t): each asteroid moves along a closed integer orbit keyed on attrs.gen_seed/phase/t, wrapped by attrs.w/attrs.h STORED AT PLACEMENT (worldgen fix — never live env); clear a docked agent if it drifts > DOCK_RANGE + event 'undock'. Asteroid respawn folded into respawn_deposits: also handle type=='asteroid' (amount<attrs.max and (t+id)%ASTEROID_RESPAWN_EVERY==0 -> +1).
decay_loot(ents,cur,t): type loot with t>=expires -> del_entity.
DEAD/DOWNED GATE (top of apply_intent after no-agent): if a.attrs.get('downed_until',0)>t and verb not in ('say','tell'): return 'rejected', downed msg. (Whitelist EXACTLY say/tell — not accept/cancel, weapons exploit.)
collect {loot:<id>} verb: adjacent dist<=1; move contents into a; del_entity; event 'collect'.

=== G. HIDDEN PEACE INCENTIVE (engine.py only; NO player-facing text; the centerpiece, reworked safe) ===
Helper _node_fortune(a,cur,t) (innocuous name, placed among gameplay helpers, commented as 'deposit richness variance'): coop = count of MARKET CROSSES the agent participated in over last KARMA_WINDOW ticks where the COUNTERPARTY differs (breadth): SELECT count(DISTINCT CASE WHEN data->>'seller'=%s THEN data->>'buyer' ELSE data->>'seller' END) FROM events WHERE kind='market' AND tick>=t-KARMA_WINDOW AND (data->>'seller'=%s OR data->>'buyer'=%s). karma=min(KARMA_MAX, coop*_NF_W_MARKET//KARMA_DIV). Fold into mine/chop took, RIGHT AFTER the storm halving, BEFORE dep.amount decrement: seed=int(sha256(f"{t}:{a.id}:{dep.x}:{dep.y}:{r}").hexdigest()[:4],16); jitter=seed%5; bonus=1 if (jitter+karma)>=COOP_THRESH else 0; took=min(have,took+bonus). karma=0 => jitter<=4<6 => bonus always 0 (zero baseline). No attr/event/notice. SECRECY: events table is prune-protected (kind='market' is not 'act', survives) so the window is queryable & replay-consistent (key off EVENTS, not pruned intents — peace_secret fix). Why market-crosses only: ping-pong trades and solo tells are removed (collusion-proof per peace_secret verdict — a market cross requires two distinct agents to both escrow at a crossing price, value-bearing; distinct-counterparty breadth still gameable by sybils but each sybil cross costs real escrowed resources at a real price, so it is no longer zero-cost). Document in the ONE private memory note (NOT in repo docs/Codex/prompts).

=== H. SERVER (app.py) ===
H1 _ensure_world: bump WORLD_W/WORLD_H env default 156->220 (env-overridable). Existing season-2 expansion branch already inserts ONLY new-region deposits (x>=gw OR y>=gh) — unchanged. Add: one-time agent hp/born migration (P3/P4); one-time asteroid placement (guard count(type='asteroid')==0, blake2b(seed:ast:i), store w/h/phase/gen_seed in attrs); one-time artifact placement (guard count(type='artifact')==0, blake2b(seed:art:kind)). All via direct INSERT (pre-tick, like worldgen.write_deposits) — they are read into ents on the first tick.
H2 worldgen.py: tundra biome gated so it ONLY classifies NEW-frontier cells (worldgen fix #2): pass a min_x/min_y to generate so biome()=='tundra' is only assigned when x>=gw or y>=gh; OR (cleaner, chosen) add tundra as the LAST elev/moist branch with a threshold (elev>0.80 and moist>0.58) tuned to fall only in the new region, AND drive _grid() cache from the SAME generate() so old-region visuals match DB. GLYPH['tundra']='%'; DEPOSITS['tundra']=[("titanium",0.06,16),("ice",0.08,18),("iron",0.03,12)] — ice amount 18 (<=respawn cap 18, worldgen fix #5). _BIOME_CODE['tundra']='%'.
H3 observe() (play.py): add nearby_agents [{id,name,x,y,hp,wanted}] within radius (e.g. 9) so attack/steal/ally are targetable (theft+alliances required_fix); add my hp/hp_max/downed_until; weapons={held weapon items + ammo counts}; alerts[] = recent 'theft'/'damage'/'attack' events where I'm the victim (last_robbed_by) so retaliation works (theft required_fix); nearby loot[] and artifacts[] and asteroids[] (if in orbit). Do NOT surface karma/yield_buff in any way.
H4 /scene: add hp/hp_max to agents/vehicles/structures; bombs[] (x,y,fuse); asteroids[] (x,y,res,amount); artifacts[] (x,y,kind,loc). /map legend: artifact glyph '!'.
H5 /milestones IN-filter: add 'war','peace','attune','destroyed' (milestone-worthy). Do NOT add 'damage'/'theft'/'attack'/'dock'/'mine' (high-frequency firehose) — keep those in /log only (theft+alliances fix). /timeline IN-filter: add 'attune'.
H6 depot base prices (engine.seed_demo + a startup UPDATE for the live depot): titanium:7, ice:1, iridium:20, nickel:5, superalloy:14, cryo_fuel:8, ion_thruster:18, gunpowder:5, slug:4, energy_cell:10, kinetic_gun:30, energy_weapon:28, bomb:9.

## BUILD ORDER
1. STEP 0 (plumbing, no behavior): add constants block; new_entity/del_entity; change apply_intent signature to (it,ents,cur,t,events) and the tick() call; add the startup hp/hp_max/born migration in _ensure_world + seed_demo + register_agent. VERIFY LIVE: server still boots, /world tick advances, /healthz running, tick-hash chain still continuous (GET /world last_state_hash changes each tick), existing verbs (mine/move/build/launch) still work. This is the riskiest change — land it alone.
2. STEP 1 (crafting, pure-fn, offline-testable): add PROPS/ITEM_PROPS/RULES/RULE_NOTE. Write a standalone pytest asserting the COLLISION PROOF set ({iron}->magnet, {iron,carbon}->steel, {steel,iron}->slug, {barrel,slug,gunpowder}->kinetic_gun, {casing,motor,gunpowder} NOT ->kinetic_gun, {gunpowder,casing}->bomb, {steel,coal}->steel, {iridium,titanium,coal}->superalloy, {ice,oil}->cryo_fuel). VERIFY: run combine() enumeration BEFORE/AFTER on every existing canonical recipe ingredient-set; zero existing recipe may newly resolve to a weapon/superalloy/etc. No engine wiring yet.
3. STEP 2 (worldgen + map grow): tundra biome (frontier-only) + DEPOSITS + glyphs; bump WORLD_W/H to 220. VERIFY LIVE: /map and /scene render the larger world; new-region tundra '%' appears; OLD-region glyphs unchanged (diff /scene biomes old rows vs season-2); existing deposits keep their amounts (no wipe); deposit count grew only in the L-region.
4. STEP 3 (HP + attack + death/respawn, the combat core): hp_of/armor/apply_damage/explode/kill_agent; attack/arm/detonate verbs; tick_bombs/respawn_agents; orbital one-shot fall; observe nearby_agents+hp+weapons+alerts; /scene hp+bombs. VERIFY LIVE with 2 throwaway agents: agent A crafts kinetic_gun+slugs, B is adjacent within range; A attack B -> B hp drops, ammo consumed, cooldown respected, out-of-ammo rejects; B downed at 0 -> loot pile spawns, B intents rejected except say/tell, respawns after 30 ticks at death cell +grace (untargetable during grace); bomb arm->fuse 3->explode caps radius<=3, dents a deposit by <=4 which self-heals; tick-hash chain stays continuous; replay a saved intent window twice -> identical hashes.
5. STEP 4 (theft): steal verb + _theft_outcome + cool_reputation; per-victim robbed_recent guard; detection-reachable math; observe alerts/last_robbed_by. VERIFY LIVE: adjacent steal moves resources (conserved), credits rejected, victim<floor rejected, cooldown enforced, a clean (undetected) heist is observably reachable (run ~20 attempts, confirm some detected=False in /log theft events), per-victim guard blocks sybil parallel-drain, rejected paths emit NO theft event.
6. STEP 5 (alliances/war, reworked): ally/accept_ally/unally/declare_war/make_peace/assist + relation rows + accrue_weariness(combat-gated)/regen_hp/expire_diplomacy; ally-block + protection gating in attack/steal; assist caps; /milestones war/peace filter; /scene relations optional. VERIFY LIVE: ally pair cannot attack/steal each other; protected newbie cannot be attacked; make_peace mints ZERO credits (confirm no credit delta — kills the sybil mint); declare_war on ally rejected; assist capped per window + credits excluded; rout/down still revives (no deletion); shove clamped in-bounds.
7. STEP 6 (worldgen layer 2/3: asteroids + artifacts): one-time placement in _ensure_world (count==0 guards, store w/h/phase/gen_seed); dock + mine-asteroid; drift_asteroids (stored dims); asteroid respawn; attune + the 3 artifact effects; observe asteroids/artifacts; /scene+/map. VERIFY LIVE: reach orbit (alt 300-599) with a flies+controllable vehicle, dock nearest asteroid within 2, mine iridium/nickel (cap 5, vacuum=no motor bonus), asteroid drifts deterministically and undocks at >2; climbing out of orbit clears docked_to (no Moon-harvest exploit); attune earth monolith = global-unique +50% yield (second agent rejected), gravity_lens halves launch gate for 200 ticks, stasis = exactly 3 decay-skips; first-to-attune awards 200 pts via 'attune' event; re-deploy with same env reproduces identical placement + tick-hash.
8. STEP 7 (hidden incentive LAST, so it never blocks combat shipping): _node_fortune market-cross karma folded into mine/chop took; private memory note. VERIFY: NOT player-facing — grep repo + /rules + /observe payload + prompts for any 'karma/peace/coop/fortune' leak (must be zero); determinism — replay a window twice => identical hashes with the nudge active; bounded — bonus only 0/1, took<=have, deposits still self-heal; baseline — an agent with zero market crosses NEVER gets the bonus (karma=0).
9. STEP 8 (regression + audit): full replay of a multi-hundred-tick window from genesis vs from-snapshot -> identical tick-hash chain (proves no lazy-HP / loot-create / drift-dim / events-vs-intents divergence). Run code-review skill on the diff. Update STATUS.md (player-facing — NO mention of the hidden incentive).

## FILES TO CHANGE
C:/Work/ideas/no-human-allowed-mmo/engine/crafting.py — new PROPS (dense tags; titanium/ice/iridium/nickel), ITEM_PROPS (gunpowder/slug/barrel/kinetic_gun/energy_cell/energy_weapon/bomb/superalloy/cryo_fuel/ion_thruster), tightened+ordered RULES with unique tags (gunbody/projectile) to fix the magnet/slug and casing/gun collisions, RULE_NOTE additions (no peace text), C:/Work/ideas/no-human-allowed-mmo/engine/engine.py — all new constants; new_entity/del_entity helpers; apply_intent signature +events; new verbs attack/arm/detonate/dock/steal/attune/ally/accept_ally/unally/declare_war/make_peace/assist/collect; mine docked-asteroid + yield_buff branches; hp_of/armor/apply_damage/explode/kill_agent; per-tick tick_bombs/respawn_agents/cool_reputation/accrue_weariness/regen_hp/expire_diplomacy/drift_asteroids/decay_loot + wiring; orbital_decay one-shot fall (require flies); _node_fortune hidden karma folded into mine/chop; relation/asteroid/artifact/loot/bomb handling; seed_demo agent hp/born + depot base price additions, C:/Work/ideas/no-human-allowed-mmo/engine/worldgen.py — tundra biome (frontier-only), GLYPH/DEPOSITS/_BIOME_CODE for tundra, ice amount<=18; ensure generate() determinism unchanged for frontier so cached grid matches DB, C:/Work/ideas/no-human-allowed-mmo/engine/play.py — observe(): nearby_agents (id/name/x/y/hp/wanted), my hp/hp_max/downed_until, weapons+ammo, alerts (victim events / last_robbed_by), nearby loot/artifacts/asteroids; NEVER expose karma/yield_buff, C:/Work/ideas/no-human-allowed-mmo/server/app.py — WORLD_W/H default 220; _ensure_world one-time hp/born migration + asteroid + artifact placement (count==0 guards, w/h/phase/gen_seed in attrs); /scene hp/bombs/asteroids/artifacts; /map artifact glyph; /milestones+/timeline kind filters; depot live-price UPDATE; registration stamps hp/hp_max/born, C:/Users/me/.claude/projects/C--Work-GITLAB-infra/memory/<new note> — PRIVATE maintainer note documenting the hidden peace incentive (_node_fortune / market-cross karma). NOT in the repo, NOT player-facing.

## ECONOMY INTEGRATION
New resources priced at the depot and tradeable on the P2P market/orders exactly like existing raws (they are ordinary buffer resources). Tiers: ground-frontier mid raws titanium(7)/ice(1) (reachable on foot in the 220x220 expansion, feed superalloy/cryo_fuel); apex orbital raws iridium(20, rarest — 4/12 asteroids, finite 30..59, cap 5/tick, vacuum disables motor bonus)/nickel(5) gated behind controllable+flying rocket->orbit->dock. Crafted combat goods are tradeable items with depot floors: gunpowder(5), slug(4, the kinetic ammo — sustained fire = sustained dense-metal crafting), energy_cell(10, the energy ammo = battery+lens tech investment), kinetic_gun(30)/energy_weapon(28)/bomb(9). Crafted tech: superalloy(14)/cryo_fuel(8)/ion_thruster(18). This binds combat to the gathering economy: total damage an agent can deal is bounded by ammo it crafted from finite (self-healing) deposits — there is NO free fire. Armor ties combat back to crafting (vehicle armor=mass//40 rewards steel/frame mass; structure armor=size rewards big builds). Loot drops (1/4 capped 50, credits excluded) recirculate a downed agent's MATERIALS without minting or dropping credits/vehicles, so death is a bounded materials setback collectable by others (a window before LOOT_TTL=120). Combat is scored on a SEPARATE combat_points field (kill=10, per-pair window-capped) that NEVER pollutes inventor_points/the inventor leaderboard. Crucially: the alliances peace-bounty credit MINT is removed (C6) — the ONLY cooperation reward is the hidden, deposit-bounded yield nudge (cannot mint, cannot be funneled). assist is a capped (50/qty, 2/window), credits-excluded ally gift channel. Artifacts award inventor_points (first-to-attune 200 = a new prestige race parallel to first-to-space, well-bounded: one global +50%-yield monolith, a 200-tick half-gravity window, 3 decay-skips — none let an agent skip building/crafting/launching).

## SAFETY CHECKLIST
- DETERMINISM: every new 'random' value is sha256(t:ids:coords[:resource])%m or staggered (t+id)%K — zero random()/time/float-RNG. All damage/HP/armor/falloff/drop/karma/weariness are integer //, min/max-clamped. Verified by from-genesis vs from-snapshot replay producing identical tick-hash chains (build step 8).
- HASH-CHAIN INTEGRITY: ALL tick-time entity creation (bomb/loot/relation/asteroid/artifact) goes through new_entity() which inserts into the in-memory ents dict BEFORE state_hash(ents) runs (fixes the death/alliances loot-create divergence the verdicts flagged). HP/hp_max/born materialized for EVERY agent/vehicle/structure at registration + a one-time startup migration (NOT lazily on damage) so serialized attrs are path-independent (fixes lazy-HP hazard). Asteroid drift uses dims STORED at placement, not live env (worldgen fix). Karma keys off the prune-surviving events table, not pruned intents (peace_secret fix).
- BOUNDED + SELF-HEALING DESTRUCTION: terrain/biome grid is procedural and NEVER written. Explosions hard-capped at radius min(aoe,3) with no agent-reachable path to exceed; per-cell bomb cap 2 (no stacked-nuke). Deposits only lose <=4 and regrow via existing respawn_deposits(18)/grow_trees(22)/asteroid-respawn(to attrs.max). Agents NEVER deleted (downed->respawn full HP at death-cell); vehicles->wrecked, structures->ruined — all kept + rebuildable (construct = materials sink). attack rejects any non-agent/vehicle/structure target. World cannot become a permanent hole.
- ANTI-EXPLOIT: n>=1/positivity guards on every quantity (negative-qty dupe blocked). All steals/loot/assist/raid are conserved transfers (addb -/+), never minted; credits never lootable/stealable/assistable. The ONLY credit mint path (alliances peace bounty) is REMOVED (C6) — kills the sybil/idle-war free-credit farm. Ammo-bounded fire = no infinite damage. Per-thief AND per-victim theft cooldowns defeat sybil parallel-drain. Protection shield (born-age < PROTECT_AGE via attrs.born, NOT the prunable events table) + RESPAWN_GRACE stop spawn/newbie camping; respawn at jittered death-cell (not the known global pad) kills death-as-teleport and camp loops. Combat points on a SEPARATE field, per-pair window-capped, can't farm idle agents or pollute the inventor leaderboard. Downed agents whitelisted to EXACTLY say/tell. assist capped+credits-excluded kills the funnel. Loop-guard + tick try/except still wrap every verb.
- HIDDEN-INCENTIVE SECRECY: lives ONLY in engine.py via an innocuously-named helper (_node_fortune, commented as deposit-richness variance), folded into the already-noisy mine/chop took (same int storms/powered-tools already move). NO new verb/entity/attr/event/notice; karma never written where observe can read it; nothing in prompts/Codex/RULE_NOTE/dashboard/STATUS.md/API. Reward IS the resource (an expected mining output). Collusion-hardened: keyed solely on market crosses (distinct-counterparty breadth, value-bearing, both-sides-escrowed) — ping-pong trades and solo tells removed. HONEST CAVEAT (documented for the owner): in an open, replay-audited engine a spectator with SQL+repo can recompute the public jitter and correlate the residual +1 with public market-cross history, so it is 'undocumented in player-facing text' but NOT information-theoretically undetectable — we minimized the surface and accept that limit (the only fully-undetectable option is to drop determinism/audit, which we do NOT). Documented in ONE private maintainer memory note outside the repo.
- REGRESSION GATE before any LIVE deploy: (a) crafting collision pytest green (no existing recipe -> weapon); (b) genesis-vs-snapshot replay hashes identical over a multi-hundred-tick window; (c) grep for hidden-incentive leakage returns zero; (d) existing verbs (mine/move/build/finalize/launch/land/combine/trade/construct/ride) unaffected; (e) /world tick + hash chain continuous after each step on the live k8s server (ns nha-mmo, NodePort :30091).

## OPEN RISKS
- SECRECY vs AUDIT is fundamentally in tension (peace_secret verdict): the hidden incentive is replay-deterministic and the engine is open/hash-audited, so it is reverse-engineerable by a spectator with repo+SQL despite no player-facing text. Owner must accept 'hidden from players in-game' rather than 'information-theoretically undetectable', OR relax determinism/audit (not recommended). Flagged, not silently resolved.
- Sybil registration is still cheap (POST /agents, no proof-of-work). We blunted the worst vectors (peace-mint removed, per-victim theft guard, born-age protection, market-cross-only karma costs real escrow per cross) but a determined multi-agent operator can still consume resources via many accounts. A registration rate-limit / proof-of-work would close it fully — out of scope for this season, recommend follow-up.
- yield_buff scope decision (earth monolith): spec applies +50% at ALL harvest sites (ground/Moon/asteroid) for consistency; if owner wants ground-only, restrict it — must be decided before STEP 6 to avoid cross-implementer non-determinism.
- Tundra threshold tuning: must be empirically verified to fall ONLY in the 156->220 frontier region against seed=42 so old-region cached-grid visuals match DB deposits (worldgen verdict fix #2). If a clean threshold isn't found, fall back to passing min_x/min_y into generate() to gate tundra strictly to new cells.
- Structure griefer-proofness: max-size structure (armor=20) takes only 1 dmg/kinetic shot vs 200 HP = ~200 slugs to ruin (intended fort defense but makes guns weak vs big builds) — documented, may need a siege/AoE-vs-structure tuning pass after observing live play.
- LIVE cluster constraints (from memory): nha-mmo k8s ns is single-node emptyDir (no dynamic storage) + pods need dnsConfig ndots:1 — schema/entity additions are additive (no migration wipe) but any redeploy that recreates the Postgres pod loses state unless the existing PVC/volume is preserved; verify before bumping WORLD_W/H so the season-2 world isn't lost.
- WORLD_W/H bump to 220 must be applied as an env change on the running deployment WITHOUT recreating the DB, relying on the existing non-wipe expansion branch; confirm gen_w/gen_h on the live market entity reads 156 so the expansion (not a regen) path triggers.